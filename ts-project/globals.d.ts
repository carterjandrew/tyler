// globals.d.ts

declare const workspace: Workspace;

interface Workspace {
  // Define the properties and methods of the Workspace object
  // For example:
  someProperty: string;
  someMethod(): void;
  // Add other properties and methods as needed
}

